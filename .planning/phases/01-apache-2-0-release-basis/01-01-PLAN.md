---
phase: 01-apache-2-0-release-basis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - NOTICE
  - docs/relicense-authorization.md
  - README.md
  - cmd/agentgw/main.go
  - internal/gateway/gateway.go
  - internal/registry/registry.go
  - internal/vault/vault.go
autonomous: false
requirements:
  - LIC-01
  - LIC-02
  - LIC-03
  - LIC-04
must_haves:
  truths:
    - "D-01, D-02: A tracked authority record names the repository, reviewed revision, effective date, owner, complete first-party scope, and 4 BSL-marked paths."
    - "D-03, D-04: Shreyansh Sancheti personally affirms authority. Git authorship and Signed-off-by remain supporting evidence only. Merge stays blocked on uncertainty."
    - "D-05, D-06: NOTICE contains exactly AgentGate and Copyright 2026 Clawdlinux. No unsupported third-party attribution is added."
    - "D-07, D-08, D-09: Exactly the 4 locked Go files receive the sibling 4-line Apache header. Their remaining bytes do not change."
    - "D-10, D-11: README keeps its License section and links concise Apache License 2.0 wording to LICENSE without legal or marketing additions."
  artifacts:
    - path: "docs/relicense-authorization.md"
      provides: "Tracked, revision-bound owner authority record with committed personal affirmation gate"
      contains: "Status: Affirmed"
    - path: "LICENSE"
      provides: "Unmodified Apache License 2.0 text"
      contains: "Apache License"
    - path: "NOTICE"
      provides: "Locked 2-line AgentGate attribution"
      contains: "Copyright 2026 Clawdlinux."
    - path: "README.md"
      provides: "Repository license statement and root LICENSE link"
      contains: "Apache License 2.0"
    - path: "cmd/agentgw/main.go"
      provides: "Apache-compatible source notice"
      contains: "Licensed under the Apache License, Version 2.0."
    - path: "internal/gateway/gateway.go"
      provides: "Apache-compatible source notice"
      contains: "Licensed under the Apache License, Version 2.0."
    - path: "internal/registry/registry.go"
      provides: "Apache-compatible source notice"
      contains: "Licensed under the Apache License, Version 2.0."
    - path: "internal/vault/vault.go"
      provides: "Apache-compatible source notice"
      contains: "Licensed under the Apache License, Version 2.0."
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "Markdown root-relative link"
      pattern: "Apache License 2\\.0.*\\[LICENSE\\]\\(LICENSE\\)"
    - from: "LICENSE"
      to: "../agentic-operator-core/LICENSE"
      via: "byte-for-byte local comparison"
      pattern: "cmp -s LICENSE ../agentic-operator-core/LICENSE"
    - from: "docs/relicense-authorization.md"
      to: "committed R1 merge approval"
      via: "blocking personal owner affirmation followed by signed-off commit"
      pattern: "git show HEAD:docs/relicense-authorization.md"
---

<objective>
Establish the Apache 2.0 source-release basis for AgentGate without changing runtime behavior.

Purpose: Clear PRD task R1 only after the owner records authority over the distributed first-party source.
Output: One reviewable licensing change covering exactly 8 source and release files, plus a blocked human merge gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<commit_policy>
Set `R1_BASELINE=414f91792751b634f81f8a8046d961d9087709da` before execution. Every R1 task commit and GSD metadata commit must use `git commit -s`. If a helper creates an unsigned commit, amend it immediately with `git commit --amend --no-edit -s`. The exact trailer is `Signed-off-by: Shreyansh Sancheti &lt;43677304+shreyanshjain7174@users.noreply.github.com&gt;`.

After each task commit, set `TASK_COMMIT=$(git rev-parse HEAD)`. Run `git show --check --oneline "$TASK_COMMIT"`. Then set `EXPECTED_SIGNOFF=$(printf 'Shreyansh Sancheti \07443677304+shreyanshjain7174@users.noreply.github.com\076')` and require `test "$(git show -s --format='%(trailers:key=Signed-off-by,valueonly)' "$TASK_COMMIT")" = "$EXPECTED_SIGNOFF"`. Before completion, repeat both checks for every commit in `R1_BASELINE..HEAD`.
</commit_policy>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-apache-2-0-release-basis/01-CONTEXT.md
@.planning/phases/01-apache-2-0-release-basis/01-RESEARCH.md
@.planning/phases/01-apache-2-0-release-basis/01-VALIDATION.md
@.planning/phases/01-apache-2-0-release-basis/01-PATTERNS.md
@PRD-receipts-oss.md
@CLAUDE.md
@.github/copilot-instructions.md
@../agentic-operator-core/LICENSE
@../agentic-operator-core/pkg/audit/chain.go

<interfaces>
This phase has no runtime interfaces. Preserve all Go bytes below each existing 7-line BSL header.

Pinned pre-R1 revision: `414f91792751b634f81f8a8046d961d9087709da`.

Expected SHA-256 values after removing lines 1 through 7 from the pre-R1 files:
- `cmd/agentgw/main.go`: `266f0d14c12fbc7f03e35cb5f39fe2d439b4fa391da7e2253b68f2045e55992c`
- `internal/gateway/gateway.go`: `b70335df4371e240579acdbf43a49acf5b4511230a89d62bde48b67608ef33a6`
- `internal/registry/registry.go`: `87b7080162b2f6429bd2dc4647714a13ec9e3bc29b7b55eee9a7ecb4e1d1669f`
- `internal/vault/vault.go`: `fdaeb2cd289ae6feb11b67d425152c3c400f85fc3117d27ca45d0a88b564bc10`

The replacement header is exactly 4 lines, followed by one blank line:
`/*`, `Copyright 2026 Clawdlinux.`, `Licensed under the Apache License, Version 2.0.`, `*/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the revision-bound authority record</name>
  <read_first>
    - `.planning/phases/01-apache-2-0-release-basis/01-CONTEXT.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-RESEARCH.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-PATTERNS.md`
    - `.planning/REQUIREMENTS.md`
    - `PRD-receipts-oss.md`
  </read_first>
  <files>docs/relicense-authorization.md</files>
  <action>Create `docs/relicense-authorization.md` per D-01 through D-04. Use heading `# AgentGate Relicense Authorization`. Add exact metadata lines for `- Repository: `github.com/Clawdlinux/agentgate``, `- Reviewed source revision: `414f91792751b634f81f8a8046d961d9087709da``, `- Effective date: `YYYY-MM-DD``, and `- Owner granting permission: Shreyansh Sancheti`. State that scope covers every distributed first-party file at the reviewed revision and the licensing-only R1 change. List the 4 paths from D-07. State that third-party works retain their own terms. Grant Apache License 2.0 permission only as a pending owner statement. Add an Owner Affirmation section with `Status: Pending personal review and affirmation.`, owner name, and an unfilled review date. State that Git authorship and `Signed-off-by` support provenance but do not establish legal authority. State that uncertainty blocks merge. Do not write `Status: Affirmed`, sign for the owner, infer authority, or add legal advice.</action>
  <verify>
    <automated>test -s docs/relicense-authorization.md &amp;&amp; rg -n '^# AgentGate Relicense Authorization$' docs/relicense-authorization.md &amp;&amp; rg -n '^- Repository: `github\.com/Clawdlinux/agentgate`$' docs/relicense-authorization.md &amp;&amp; rg -n '^- Reviewed source revision: `414f91792751b634f81f8a8046d961d9087709da`$' docs/relicense-authorization.md &amp;&amp; rg -n '^- Effective date: `[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])`$' docs/relicense-authorization.md &amp;&amp; rg -n '^- Owner granting permission: Shreyansh Sancheti$' docs/relicense-authorization.md &amp;&amp; rg -n '^Status: Pending personal review and affirmation\.$' docs/relicense-authorization.md &amp;&amp; rg -n '^Reviewed by: Shreyansh Sancheti$' docs/relicense-authorization.md &amp;&amp; rg -n '^Review date: `&lt;YYYY-MM-DD&gt;`$' docs/relicense-authorization.md &amp;&amp; rg -n 'Apache License 2\.0' docs/relicense-authorization.md &amp;&amp; rg -n 'Signed-off-by' docs/relicense-authorization.md &amp;&amp; rg -n 'blocks merge' docs/relicense-authorization.md &amp;&amp; for file in cmd/agentgw/main.go internal/gateway/gateway.go internal/registry/registry.go internal/vault/vault.go; do rg -F -- "$(printf '\140')${file}$(printf '\140')" docs/relicense-authorization.md &gt;/dev/null || exit 1; done &amp;&amp; ! rg -n '^Status: Affirmed$' docs/relicense-authorization.md &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: D-01 and LIC-01 are represented by a tracked, non-empty authority record.
    - Content assertion: D-02 fields and all 4 D-07 paths appear verbatim.
    - Behavior assertion: D-03 leaves personal affirmation pending and rejects authorship or sign-off as authority.
    - Gate assertion: D-04 explicitly blocks merge when authority is incomplete or uncertain.
    - Command assertion: the automated verification exits 0 without changing product source.
  </acceptance_criteria>
  <done>The pending authority record contains every required scope field and makes no owner affirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the exact Apache source-release metadata</name>
  <read_first>
    - `README.md`
    - `cmd/agentgw/main.go`
    - `internal/gateway/gateway.go`
    - `internal/registry/registry.go`
    - `internal/vault/vault.go`
    - `../agentic-operator-core/LICENSE`
    - `../agentic-operator-core/pkg/audit/chain.go`
    - `.planning/phases/01-apache-2-0-release-basis/01-CONTEXT.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-PATTERNS.md`
  </read_first>
  <files>LICENSE, NOTICE, README.md, cmd/agentgw/main.go, internal/gateway/gateway.go, internal/registry/registry.go, internal/vault/vault.go</files>
  <action>Copy `../agentic-operator-core/LICENSE` byte for byte to root `LICENSE`. Create root `NOTICE` with exactly `AgentGate` then `Copyright 2026 Clawdlinux.` on the next line, with one final newline, per D-05 and D-06. In each D-07 file, replace only the existing 7-line BSL block with the exact 4-line header from `../agentic-operator-core/pkg/audit/chain.go`, retaining the blank line before existing content per D-08. Do not add headers to any other Go file per D-09. In the existing README `## License` section, replace only the BSL sentence with `AgentGate is licensed under the Apache License 2.0. See [LICENSE](LICENSE).` per D-10. Add no interpretation, patent summary, badge, launch copy, dependency name, package, formatting change, or runtime edit per D-06 and D-11. Limit this task to the listed 7 files.</action>
  <verify>
    <automated>cmp -s LICENSE ../agentic-operator-core/LICENSE &amp;&amp; printf 'AgentGate\nCopyright 2026 Clawdlinux.\n' | cmp -s - NOTICE &amp;&amp; header=$'/*\nCopyright 2026 Clawdlinux.\nLicensed under the Apache License, Version 2.0.\n*/\n\n' &amp;&amp; for file in cmd/agentgw/main.go internal/gateway/gateway.go internal/registry/registry.go internal/vault/vault.go; do printf '%s' "$header" | cmp -s - &lt;(sed -n '1,5p' "$file") || exit 1; done &amp;&amp; expected=$'cmd/agentgw/main.go\ninternal/gateway/gateway.go\ninternal/registry/registry.go\ninternal/vault/vault.go' &amp;&amp; actual=$(git ls-files '*.go' | xargs rg -l 'Licensed under the Apache License, Version 2\.0\.' | sort) &amp;&amp; test "$actual" = "$expected" &amp;&amp; ! git ls-files '*.go' | xargs rg -n 'Business Source License|Business Source|BSL' &amp;&amp; test "$(sed '1,5d' cmd/agentgw/main.go | shasum -a 256 | awk '{print $1}')" = '266f0d14c12fbc7f03e35cb5f39fe2d439b4fa391da7e2253b68f2045e55992c' &amp;&amp; test "$(sed '1,5d' internal/gateway/gateway.go | shasum -a 256 | awk '{print $1}')" = 'b70335df4371e240579acdbf43a49acf5b4511230a89d62bde48b67608ef33a6' &amp;&amp; test "$(sed '1,5d' internal/registry/registry.go | shasum -a 256 | awk '{print $1}')" = '87b7080162b2f6429bd2dc4647714a13ec9e3bc29b7b55eee9a7ecb4e1d1669f' &amp;&amp; test "$(sed '1,5d' internal/vault/vault.go | shasum -a 256 | awk '{print $1}')" = 'fdaeb2cd289ae6feb11b67d425152c3c400f85fc3117d27ca45d0a88b564bc10' &amp;&amp; rg -n '^## License$' README.md &amp;&amp; rg -n 'Apache License 2\.0.*\[LICENSE\]\(LICENSE\)' README.md &amp;&amp; go test ./cmd/agentgw ./internal/gateway ./internal/registry ./internal/vault &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - LIC-02 and T-02: `cmp -s` proves the root license matches the reviewed sibling byte for byte.
    - D-05, D-06, and T-03: byte comparison proves NOTICE is exactly 2 locked lines plus one final newline.
    - LIC-03 and D-07 through D-09: byte comparison proves each complete 5-line header, including its blank line. The path set equals the 4 locked files, and no tracked Go file contains BSL text.
    - Scope assertion: all 4 post-header SHA-256 values match the pinned pre-R1 bodies, proving no runtime bytes changed.
    - LIC-04, D-10, D-11, and T-04: README retains `## License`, names Apache License 2.0, and links root `LICENSE` without extra claims.
    - Regression assertion: focused Go tests and `git diff --check` pass.
  </acceptance_criteria>
  <done>The 7 mechanical files match the locked Apache metadata, exact path scope, unchanged Go bodies, and focused regression checks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Record the owner's personal authority affirmation</name>
  <read_first>
    - `docs/relicense-authorization.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-CONTEXT.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-VALIDATION.md`
    - `.planning/REQUIREMENTS.md`
    - `PRD-receipts-oss.md`
  </read_first>
  <files>docs/relicense-authorization.md</files>
  <action>Stop before merge. Shreyansh Sancheti must personally review the final tracked authority record and R1 file set. Only Shreyansh may change `Status: Pending personal review and affirmation.` to `Status: Affirmed`, fill the review date, and confirm the statement. Do not infer approval from Git history, a DCO trailer, silence, `workflow.auto_advance`, or another person's response. If ownership, assignment, imported-code rights, repository scope, or permission is uncertain, leave status pending and keep Phase 1 blocked per D-03 and D-04.</action>
  <verify>
    <automated>test -s docs/relicense-authorization.md &amp;&amp; rg -n '^Status: Affirmed$' docs/relicense-authorization.md &amp;&amp; rg -n '^Reviewed by: Shreyansh Sancheti$' docs/relicense-authorization.md &amp;&amp; rg -n '^Review date: [0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])$' docs/relicense-authorization.md &amp;&amp; ! rg -n '^Status: Pending personal review and affirmation\.$|&lt;YYYY-MM-DD&gt;' docs/relicense-authorization.md &amp;&amp; git diff --check</automated>
    <human-check>Shreyansh Sancheti confirms the repository, pinned revision plus R1 diff, complete first-party scope, 4 BSL paths, effective date, Apache 2.0 permission, typed name, and review date. He personally records `Status: Affirmed`. Any uncertainty blocks merge.</human-check>
  </verify>
  <acceptance_criteria>
    - LIC-01 and T-01: personal owner affirmation is recorded in the tracked file before merge.
    - D-03: affirmation comes from Shreyansh Sancheti, not authorship or `Signed-off-by` evidence.
    - D-04: pending or uncertain authority blocks this plan and every downstream phase.
    - Human assertion: the owner has reviewed the final R1 scope and entered the review date personally.
    - Command assertion: automated text and whitespace checks pass after the owner's edit.
  </acceptance_criteria>
  <done>Shreyansh Sancheti personally records affirmation and the review date, or the phase remains blocked.</done>
  <resume-signal>Only Shreyansh Sancheti may reply `affirmed` after personally updating the tracked record. Otherwise describe the authority uncertainty and keep the gate blocked.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Commit and verify the owner's affirmation</name>
  <read_first>
    - `docs/relicense-authorization.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-CONTEXT.md`
    - `.planning/phases/01-apache-2-0-release-basis/01-VALIDATION.md`
    - `CLAUDE.md`
    - `.github/copilot-instructions.md`
  </read_first>
  <files>docs/relicense-authorization.md</files>
  <action>After Task 3 resumes, run in zsh on macOS. Create a temporary directory with `mktemp -d "${TMPDIR%/}/agentgate-authority.XXXXXX"` and install a trap that removes it on exit or interruption. Define one shared `validate_authority_transform parent_blob current_blob` shell function and use that exact function for the fresh-commit precheck, reused-HEAD detection, and final committed-HEAD check. The function must require exactly one `Status: Affirmed`, `Reviewed by: Shreyansh Sancheti`, and calendar-shaped `Review date: YYYY-MM-DD` in `current_blob`. Extract `review_date` from that blob. Parse it with BSD `date -j -f '%Y-%m-%d' "$review_date" '+%Y-%m-%d'` and require the output to equal the input. Do not use GNU-only `date -d`. The function must require exactly one `Status: Pending personal review and affirmation.` and exactly one ``Review date: `&lt;YYYY-MM-DD&gt;` `` in `parent_blob`. Build a temporary expected blob from the parent by replacing only those 2 exact lines. Byte-compare it with `current_blob` using `cmp -s`. This rejects impossible dates, placeholders, extra edits, duplicate fields, and newline changes.</action>
  <action>For reuse detection, write `git show HEAD^:docs/relicense-authorization.md` and `git show HEAD:docs/relicense-authorization.md` to temporary blobs. Reuse `HEAD` only if the shared validator passes, `git diff-tree --no-commit-id --name-only -r HEAD` equals exactly `docs/relicense-authorization.md`, the authority file has no worktree or index change, and `HEAD` has the exact `Signed-off-by: Shreyansh Sancheti &lt;43677304+shreyanshjain7174@users.noreply.github.com&gt;` trailer. Otherwise take the fresh path. Before a fresh commit, require `git diff --name-only` to equal exactly `docs/relicense-authorization.md` and require an empty index. Write `git show HEAD:docs/relicense-authorization.md` to the parent blob. Run the same shared validator against that parent blob and the working-tree file. Do not edit, normalize, or rewrite the owner's affirmation. Stage only `docs/relicense-authorization.md`. Require `git diff --cached --name-only` to equal that path. Run exactly `git commit -s -m "docs: record relicense affirmation"`.</action>
  <action>After either path, rerun all checks against the commit. Require `git diff-tree --no-commit-id --name-only -r HEAD` to equal exactly the authority path. Read the parent and current committed blobs with `git show HEAD^:docs/relicense-authorization.md` and `git show HEAD:docs/relicense-authorization.md`. Run the same shared validator against them. Require no worktree or index change for the authority file. Recheck the exact sign-off trailer and run `git show --check --oneline HEAD`. Remove all temporary files through the trap. Follow the project signed-off commit rules. Do not commit any other path.</action>
  <verify>
    <automated>tmp_root=$(mktemp -d "${TMPDIR%/}/agentgate-authority.XXXXXX") &amp;&amp; trap 'rm -rf "$tmp_root"' EXIT HUP INT TERM &amp;&amp; validate_authority_transform() { parent_blob="$1"; current_blob="$2"; expected_blob="$tmp_root/expected"; test "$(grep -Fxc 'Status: Affirmed' "$current_blob")" -eq 1 &amp;&amp; test "$(grep -Fxc 'Reviewed by: Shreyansh Sancheti' "$current_blob")" -eq 1 &amp;&amp; test "$(grep -Ec '^Review date: [0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])$' "$current_blob")" -eq 1 &amp;&amp; review_date=$(sed -nE 's/^Review date: ([0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01]))$/\1/p' "$current_blob") &amp;&amp; parsed_date=$(date -j -f '%Y-%m-%d' "$review_date" '+%Y-%m-%d' 2&gt;/dev/null) &amp;&amp; test "$parsed_date" = "$review_date" &amp;&amp; test "$(grep -Fxc 'Status: Pending personal review and affirmation.' "$parent_blob")" -eq 1 &amp;&amp; test "$(grep -Fxc 'Review date: `&lt;YYYY-MM-DD&gt;`' "$parent_blob")" -eq 1 &amp;&amp; sed -e 's|^Status: Pending personal review and affirmation\.$|Status: Affirmed|' -e 's|^Review date: `&lt;YYYY-MM-DD&gt;`$|Review date: '"$review_date"'|' "$parent_blob" &gt; "$expected_blob" &amp;&amp; cmp -s "$expected_blob" "$current_blob"; } &amp;&amp; git show HEAD^:docs/relicense-authorization.md &gt; "$tmp_root/parent" &amp;&amp; git show HEAD:docs/relicense-authorization.md &gt; "$tmp_root/current" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD)" = 'docs/relicense-authorization.md' &amp;&amp; validate_authority_transform "$tmp_root/parent" "$tmp_root/current" &amp;&amp; test -z "$(git status --short -- docs/relicense-authorization.md)" &amp;&amp; EXPECTED_SIGNOFF=$(printf 'Shreyansh Sancheti \07443677304+shreyanshjain7174@users.noreply.github.com\076') &amp;&amp; test "$(git show -s --format='%(trailers:key=Signed-off-by,valueonly)' HEAD)" = "$EXPECTED_SIGNOFF" &amp;&amp; git show --check --oneline HEAD</automated>
  </verify>
  <acceptance_criteria>
    - LIC-01 and T-01: the owner-authored affirmation and real calendar date are present in the committed `HEAD` blob.
    - Scope assertion: byte comparison proves the committed blob differs from its parent only by the 2 exact status and review-date transitions.
    - Commit assertion: `git diff-tree` proves the affirmation commit changes only `docs/relicense-authorization.md`.
    - Provenance assertion: `HEAD` contains the exact required `Signed-off-by` trailer and passes `git show --check`.
    - Idempotency assertion: only an exact-path, exact-transform, real-date, signed-off `HEAD` commit is reused without duplication.
  </acceptance_criteria>
  <done>The exact 2-field owner affirmation is committed once, contains a real calendar date, changes only the authority path, and is verified from `HEAD`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner affirmation to tracked release record | A human legal representation enters repository history. Automation cannot establish it. |
| Reviewed sibling license to AgentGate root license | Canonical terms cross a repository boundary and must remain byte-identical. |
| Source inventory to NOTICE and headers | Attribution and license labels must match the actual source artifact without omission or invention. |
| README statement to root LICENSE | Human-facing license wording must resolve to the authoritative local terms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Spoofing | `docs/relicense-authorization.md` | mitigate | Require Shreyansh Sancheti's personal tracked affirmation. Block merge and downstream phases while status is pending or authority is uncertain. |
| T-02 | Tampering | `LICENSE` | mitigate | Copy the reviewed sibling Apache license and require `cmp -s LICENSE ../agentic-operator-core/LICENSE`. |
| T-03 | Repudiation | `NOTICE` and source inventory | mitigate | Lock NOTICE to 2 factual lines. Require artifact-specific evidence before adding any third-party notice. Scan the exact first-party Go path set. |
| T-04 | Spoofing | `README.md` license claim | mitigate | Use exact Apache License 2.0 wording and link it directly to root `[LICENSE](LICENSE)`. |
</threat_model>

<source_coverage>
SOURCE | ID | Feature or requirement | Plan | Status
GOAL | phase-01 | Inspectable Apache 2.0 release basis after owner confirmation | 01-01 | COVERED
REQ | LIC-01 | Owner authority over distributed first-party files | 01-01 Tasks 1, 3, and 4 | COVERED
REQ | LIC-02 | Canonical LICENSE and accurate NOTICE | 01-01 Task 2 | COVERED
REQ | LIC-03 | Exact Apache Go notices and no BSL residue | 01-01 Task 2 | COVERED
REQ | LIC-04 | README Apache 2.0 statement and LICENSE link | 01-01 Task 2 | COVERED
RESEARCH | authority | Revision-bound personal affirmation, signed-off persistence, and blocked merge | 01-01 Tasks 1, 3, and 4 | COVERED
RESEARCH | release-files | Byte-identical license and minimal NOTICE | 01-01 Task 2 | COVERED
RESEARCH | source-integrity | Exact 4-path header replacement with unchanged bodies | 01-01 Task 2 | COVERED
RESEARCH | validation | Focused tests, committed authority checks, full suite, archive, and text checks | 01-01 Task 4 and verification | COVERED
CONTEXT | D-01..D-04 | Authority record, non-automatable owner gate, and committed affirmation | 01-01 Tasks 1, 3, and 4 | COVERED
CONTEXT | D-05..D-06 | Locked minimal NOTICE | 01-01 Task 2 | COVERED
CONTEXT | D-07..D-09 | Exact 4 Go headers and no broad churn | 01-01 Task 2 | COVERED
CONTEXT | D-10..D-11 | Narrow README wording without added claims | 01-01 Task 2 | COVERED
</source_coverage>

<verification>
Run this final phase verification only after Task 4 completes. Set `R1_BASELINE=414f91792751b634f81f8a8046d961d9087709da`:

1. `go test ./...`
2. `git diff --check "$R1_BASELINE...HEAD" -- . ':(exclude).planning/**'`
3. Rerun Task 4's shared committed-blob validator. Require `git diff-tree --no-commit-id --name-only -r HEAD` to equal exactly `docs/relicense-authorization.md`. Extract the review date from `git show HEAD:docs/relicense-authorization.md`. Require BSD `date -j -f '%Y-%m-%d' "$review_date" '+%Y-%m-%d'` to succeed and round-trip exactly. Build the expected committed blob from `HEAD^` by replacing only the exact pending-status and placeholder-date lines. Require it to be byte-identical to the `HEAD` blob. Impossible dates, placeholders, any third field transition, or any second path fail.
4. `cmp -s LICENSE ../agentic-operator-core/LICENSE`
5. `printf 'AgentGate\nCopyright 2026 Clawdlinux.\n' | cmp -s - NOTICE`
6. Set `header=$'/*\nCopyright 2026 Clawdlinux.\nLicensed under the Apache License, Version 2.0.\n*/\n\n'`. For each locked Go file, require `printf '%s' "$header" | cmp -s - &lt;(sed -n '1,5p' "$file")`.
7. `expected=$'cmd/agentgw/main.go\ninternal/gateway/gateway.go\ninternal/registry/registry.go\ninternal/vault/vault.go'; actual=$(git ls-files '*.go' | xargs rg -l 'Licensed under the Apache License, Version 2\.0\.' | sort); test "$actual" = "$expected"`
8. `! git ls-files '*.go' | xargs rg -n 'Business Source License|Business Source|BSL'`
9. `rg -n '^## License$' README.md &amp;&amp; rg -n 'Apache License 2\.0.*\[LICENSE\]\(LICENSE\)' README.md`
10. Recompute all 4 pinned post-header SHA-256 values. Require the values in `<interfaces>` exactly.
11. Create a temporary tar with `git archive --format=tar --output="$TMPDIR/agentgate-r1.tar" HEAD`. Confirm `LICENSE`, `NOTICE`, and `docs/relicense-authorization.md` each appear exactly once. Remove the tar.
12. Require exactly the 8 implementation paths outside planning metadata: `expected=$(printf '%s\n' LICENSE NOTICE README.md cmd/agentgw/main.go docs/relicense-authorization.md internal/gateway/gateway.go internal/registry/registry.go internal/vault/vault.go | sort); actual=$(git diff --name-only "$R1_BASELINE...HEAD" -- . ':(exclude).planning/**' | sort); test "$actual" = "$expected"`.
13. Check every R1 task and metadata commit: `EXPECTED_SIGNOFF=$(printf 'Shreyansh Sancheti \07443677304+shreyanshjain7174@users.noreply.github.com\076'); for commit in $(git rev-list "$R1_BASELINE..HEAD"); do git show --check --oneline "$commit" &gt;/dev/null || exit 1; test "$(git show -s --format='%(trailers:key=Signed-off-by,valueonly)' "$commit")" = "$EXPECTED_SIGNOFF" || exit 1; done`.
14. Confirm Shreyansh Sancheti personally entered the exact affirmation fields in Task 3. Confirm Task 4 persisted those unchanged fields in `HEAD`. Personal affirmation cannot be automated or auto-approved.
</verification>

<success_criteria>
- LIC-01 through LIC-04 all pass.
- D-01 through D-11 are reflected in tracked artifacts and acceptance checks.
- The final committed R1 implementation diff, excluding `.planning/**`, touches exactly the 8 scoped source and release files.
- `git show HEAD:docs/relicense-authorization.md` contains the exact affirmed status, reviewer, and real BSD-calendar-validated review date recorded by the owner.
- The affirmation commit changes only the authority path and exactly 2 fields. Its `HEAD` blob byte-matches the expected transformation from `HEAD^`.
- Reuse occurs only when the existing `HEAD` passes the same exact-path, exact-transform, real-date, sign-off, and whitespace checks as a fresh commit.
- Every R1 task and metadata commit has the exact required `Signed-off-by` trailer and passes `git show --check`.
- Root LICENSE matches the reviewed Apache 2.0 text byte for byte.
- NOTICE is exactly the locked 2-line identity.
- Exactly 4 named Go files carry the exact Apache header. Their bodies remain byte-identical to the pinned baseline.
- No tracked first-party Go file contains Business Source License text.
- README names Apache License 2.0 and links root LICENSE without added legal or marketing claims.
- Full Go tests and release checks pass.
- The source archive contains LICENSE, NOTICE, and the authority record.
- Phase 1 remains blocked until Shreyansh Sancheti personally records affirmation and Task 4 commits it with the exact sign-off. Downstream phases remain blocked until then.
</success_criteria>

<output>
Create `.planning/phases/01-apache-2-0-release-basis/01-01-SUMMARY.md` when execution finishes.
</output>
