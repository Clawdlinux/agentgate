---
phase: 02-receipt-protocol
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - internal/receipt/encoding.go
  - internal/receipt/encoding_test.go
  - internal/receipt/receipt_external_test.go
  - internal/receipt/golden_external_test.go
  - internal/receipt/fuzz_test.go
  - internal/receipt/testdata/v1/manifest.json
  - internal/receipt/testdata/v1/genesis-unicode-max.bin
  - internal/receipt/testdata/v1/gen/main.go
autonomous: true
requirements:
  - RCPT-01
  - RCPT-04
  - RCPT-05
must_haves:
  truths:
    - "D-01: D-02: D-03: D-04: D-05: Canonical bytes are one domain-separated entry-hash preimage whose SHA-256 excludes derived fields."
    - "D-06: D-07: D-08: D-09: D-10: D-11: Binary widths, little-endian order, exact UTF-8 bytes, ordered delegation, and validated bounds are fixed."
    - "D-20 through D-22: Production operations are pure, and a black-box encoder independently reconstructs every byte."
    - "D-23 and D-24: Reviewed manifest and binary fixtures cover required boundaries and cannot be rewritten by tests."
  artifacts:
    - path: "internal/receipt/encoding.go"
      provides: "Canonical binary preimage and SHA-256 entry hash"
      exports: ["HashDomainV1", "CanonicalHashInput", "ComputeEntryHash"]
    - path: "internal/receipt/receipt_external_test.go"
      provides: "Independent external-package encoder agreement and golden checks"
    - path: "internal/receipt/fuzz_test.go"
      provides: "Digest and binary property fuzz targets"
      contains: "FuzzDigestParams"
    - path: "internal/receipt/testdata/v1/manifest.json"
      provides: "Human-readable protocol inputs, boundaries, lengths, and hashes"
    - path: "internal/receipt/testdata/v1/genesis-unicode-max.bin"
      provides: "Immutable canonical entry-hash preimage fixture"
    - path: "internal/receipt/testdata/v1/gen/main.go"
      provides: "Explicit fixture generator with exclusive no-overwrite writes"
  key_links:
    - from: "internal/receipt/encoding.go"
      to: "internal/receipt/receipt.go"
      via: "private snapshot validation before any append"
      pattern: "Validate"
    - from: "internal/receipt/receipt_external_test.go"
      to: "internal/receipt/encoding.go"
      via: "byte equality without production encoding calls"
      pattern: "TestReferenceEncoderAgreement"
    - from: "internal/receipt/receipt_external_test.go"
      to: "internal/receipt/testdata/v1/genesis-unicode-max.bin"
      via: "read-only binary comparison and fixed SHA-256"
      pattern: "TestGoldenFixtures"
---

<objective>
Freeze the canonical entry-hash bytes through independent encoding, fuzzing, and immutable fixtures.

Purpose: Give implementers and auditors a byte contract that does not depend on one implementation.
Output: Production encoder, entry hash, black-box encoder, fuzz targets, manifest, binary golden, and no-overwrite generator.

This plan completes the second execution slice inside the same future R2 pull request.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-receipt-protocol/02-CONTEXT.md
@.planning/phases/02-receipt-protocol/02-RESEARCH.md
@.planning/phases/02-receipt-protocol/02-VALIDATION.md
@.planning/phases/02-receipt-protocol/02-PATTERNS.md
@.planning/phases/02-receipt-protocol/02-01-SUMMARY.md
@PRD-receipts-oss.md
@CLAUDE.md
@.github/copilot-instructions.md
@internal/receipt/receipt.go
@internal/receipt/receipt_test.go
@internal/receipt/params.go
@internal/receipt/params_test.go

<interfaces>
Consume the exact Receipt type created by 02-01.

Expose these pure production contracts:

const HashDomainV1 = "agentgate.receipt.hash.v1"
func CanonicalHashInput(receipt Receipt) ([]byte, error)
func ComputeEntryHash(receipt Receipt) ([32]byte, error)

The exact binary grammar is:

ASCII("agentgate.receipt.hash.v1") || 0x00
LE64(seq)
LE64(timestamp_unix_ns)
LP32(human_principal)
LP32(agent_key_id)
LE32(delegation_count)
repeat delegation_count times: LP32(delegation_element)
LP32(service)
LP32(action)
params_sha256[32]
LP32(policy_decision)
LE32(int32(status_code))
LE64(int64(latency_ms))
LP32(error)
prev_hash[32]
LP32(signer_kid)

`LP32(x)` is `LE32(len(UTF8(x))) || UTF8(x)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the canonical binary preimage and entry hash</name>
  <read_first>
    .planning/phases/02-receipt-protocol/02-CONTEXT.md
    .planning/phases/02-receipt-protocol/02-PATTERNS.md
    internal/receipt/receipt.go
    internal/receipt/receipt_test.go
    ../agentic-operator-core/pkg/audit/chain.go
  </read_first>
  <files>internal/receipt/encoding.go, internal/receipt/encoding_test.go</files>
  <behavior>
    - Canonical bytes begin with exact domain bytes and one NUL.
    - Every included field appears once in the locked order and width.
    - EntryHash and Signature changes do not alter canonical bytes.
    - Every other field mutation changes canonical bytes.
    - ComputeEntryHash equals SHA-256 over the complete canonical input.
    - Calls return fresh bytes and never mutate Receipt or DelegationChain.
  </behavior>
  <action>
Write binary contract tests first. Confirm focused tests fail. Implement the Apache-licensed `encoding.go` with `HashDomainV1`, `CanonicalHashInput`, and `ComputeEntryHash`.

Snapshot the Receipt and clone DelegationChain. Validate that snapshot before appending any byte. Start with exact ASCII `agentgate.receipt.hash.v1`, then one NUL byte. Append fields after the domain in this exact order: `seq`, `timestamp_unix_ns`, `human_principal`, `agent_key_id`, `delegation_chain`, `service`, `action`, `params_sha256`, `policy_decision`, `status_code`, `latency_ms`, `error`, `prev_hash`, `signer_kid`.

Encode `uint64` values as 8-byte little-endian. Encode status as signed 4-byte little-endian only after range validation. Encode latency as signed 8-byte little-endian. Encode strings as uint32 little-endian byte length followed by exact UTF-8 bytes. Encode DelegationChain as uint32 little-endian element count followed by ordered length-prefixed elements. Append ParamsSHA256 and PrevHash as exactly 32 bytes without length prefixes.

Exclude EntryHash and Signature. Include SignerKID. Return a fresh byte slice. ComputeEntryHash returns SHA-256 over the complete canonical input. Do not copy HMAC, mutation, clocks, payload retention, logging, storage, or signing behavior from the reference.
  </action>
  <verify>
    <automated>go test ./internal/receipt -run 'Test(CanonicalHashInput|ComputeEntryHash)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - D-01: CanonicalHashInput returns only entry-hash preimage bytes. It defines no persistence or JSONL format.
    - D-02: Every preimage starts with exact domain ASCII and one NUL byte.
    - D-03: The 14 fields follow the exact locked order copied into this task.
    - D-04: EntryHash and Signature are excluded. SignerKID is included.
    - D-05: ComputeEntryHash returns SHA-256 over the complete canonical input.
    - D-06: Integer widths and little-endian encodings are exact.
    - D-07: LP32 uses exact UTF-8 byte lengths and bytes without normalization.
    - D-08: Delegation count and ordered LP32 elements are exact.
    - D-09 through D-11: Validation runs before narrowing, sizing, or appending.
    - D-20: Public operations are pure, return errors, clone caller slices, and return fresh bytes.
    - No HMAC, Ed25519, database, gateway, audit logger, export, or CLI code is added.
  </acceptance_criteria>
  <done>Every listed acceptance criterion passes, and the automated verification exits zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the independent encoder and fuzz properties</name>
  <read_first>
    .planning/phases/02-receipt-protocol/02-CONTEXT.md
    .planning/phases/02-receipt-protocol/02-RESEARCH.md
    .planning/phases/02-receipt-protocol/02-VALIDATION.md
    internal/receipt/receipt.go
    internal/receipt/params.go
    internal/receipt/encoding.go
    ../agentic-operator-core/pkg/audit/audit_test.go
  </read_first>
  <files>internal/receipt/receipt_external_test.go, internal/receipt/fuzz_test.go</files>
  <behavior>
    - An external-package encoder reproduces production bytes for minimal and maximum valid receipts.
    - The reference encoder validates its own assumptions and appends each field itself.
    - Digest and binary fuzz targets never panic and produce repeatable outcomes.
    - Included mutations affect bytes. Derived-field mutations do not.
    - Returned preimages do not alias caller slices.
  </behavior>
  <action>
Create `receipt_external_test.go` as `package receipt_test`. Implement a black-box reference encoder using only public Receipt fields and protocol constants. Append all fields independently in the exact grammar above. Do not call production `Validate`, `CanonicalHashInput`, `ComputeEntryHash`, or any private helper. Compare reference bytes with production bytes before comparing hashes.

Add `FuzzDigestParams` and `FuzzCanonicalHashInput` using standard Go fuzzing. Seed every deterministic category from 02-RESEARCH.md. Include equivalent objects, decoded duplicate names, Unicode failures, depth 32 and 33, canonical size boundaries, binary64 boundaries, empty forms, trailing JSON, and minimal and maximum receipts.

Assert no panic. Repeated input must return the same value or stable error category. Successful canonical calls must return fresh equal bytes. Included-field changes alter preimages. EntryHash and Signature changes do not. Caller slice mutation cannot change already returned bytes. Do not add a fuzzing dependency.
  </action>
  <verify>
    <automated>go test ./internal/receipt -run 'TestReferenceEncoderAgreement' -count=1 &amp;&amp; go test ./internal/receipt -run '^$' -fuzz '^FuzzDigestParams$' -fuzztime=10s &amp;&amp; go test ./internal/receipt -run '^$' -fuzz '^FuzzCanonicalHashInput$' -fuzztime=10s &amp;&amp; reference_body="$(sed -n '/^func referenceEncode/,/^}/p' internal/receipt/receipt_external_test.go)" &amp;&amp; ! printf '%s\n' "$reference_body" | grep -E 'receipt\.(Validate|CanonicalHashInput|ComputeEntryHash)'</automated>
  </verify>
  <acceptance_criteria>
    - D-20: Fuzz properties prove pure repeatable operations and caller-slice isolation.
    - D-22: The reference encoder is in package receipt_test and shares no production encoding calls or private helpers.
    - The reference encoder independently applies the exact domain, order, widths, lengths, counts, and exclusions.
    - Both 10-second fuzz commands pass without panic or nondeterministic results.
    - Parser and binary boundary seeds cover T-01, T-02, and T-05.
  </acceptance_criteria>
  <done>Every listed acceptance criterion passes, and the automated verification exits zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze immutable manifest and binary fixtures</name>
  <read_first>
    .planning/phases/02-receipt-protocol/02-CONTEXT.md
    .planning/phases/02-receipt-protocol/02-RESEARCH.md
    .planning/phases/02-receipt-protocol/02-VALIDATION.md
    .planning/phases/02-receipt-protocol/02-PATTERNS.md
    internal/receipt/receipt.go
    internal/receipt/encoding.go
    internal/receipt/receipt_external_test.go
  </read_first>
  <files>internal/receipt/testdata/v1/manifest.json, internal/receipt/testdata/v1/genesis-unicode-max.bin, internal/receipt/testdata/v1/gen/main.go, internal/receipt/golden_external_test.go</files>
  <behavior>
    - The fixture uses maximum sequence, timestamp, and latency values with status 599.
    - PrevHash is 32 zero bytes. DelegationChain is empty.
    - A string contains exact unnormalized multibyte UTF-8 bytes.
    - Nonzero derived fields in the manifest do not affect the binary preimage.
    - Manifest length and lowercase SHA-256 match the checked-in binary.
    - Tests read fixtures without writing. The generator refuses every existing destination.
  </behavior>
  <action>
Write failing read-only golden checks in `golden_external_test.go`. Then create an explicit `testdata/v1/gen/main.go` generator. Give it a required `-out` directory. Its independent encoder must not import production Validate, CanonicalHashInput, ComputeEntryHash, or private helpers. It must duplicate the public binary grammar itself.

Create each destination with `os.O_CREATE|os.O_EXCL|os.O_WRONLY` and mode `0o644`. Refuse any existing destination. Add no update flag, environment override, `go:generate`, overwrite path, or test invocation.

Generate one valid `genesis-unicode-max` fixture. Use maximum uint64 sequence and timestamp, maximum int64 latency, status 599, zero PrevHash, empty DelegationChain, and exact unnormalized multibyte UTF-8. Include nonzero EntryHash and Signature values in the manifest to prove exclusion. The binary contains only canonical preimage bytes.

Create a stable 2-space manifest. Include protocol version, exact domain, receipt fields, binary filename, byte length, and lowercase SHA-256. Represent precision-sensitive integers as decimal strings. Describe invalid validation boundaries without binary files. Include no raw parameter JSON, OAuth token, upstream body, or provider error text.

Run the generator once for the reviewed fixture. Run it again and require failure without changes. Never invoke it from tests. Golden tests must load the manifest and binary read-only. They must verify production bytes, independent bytes, documented length, and documented SHA-256.
  </action>
  <verify>
    <automated>( tmp_dir="$(mktemp -d)" &amp;&amp; trap 'rm -rf "$tmp_dir"' EXIT HUP INT TERM &amp;&amp; go run ./internal/receipt/testdata/v1/gen -out "$tmp_dir" &amp;&amp; before_hashes="$(shasum -a 256 "$tmp_dir/manifest.json" "$tmp_dir/genesis-unicode-max.bin")" &amp;&amp; ! go run ./internal/receipt/testdata/v1/gen -out "$tmp_dir" &amp;&amp; after_hashes="$(shasum -a 256 "$tmp_dir/manifest.json" "$tmp_dir/genesis-unicode-max.bin")" &amp;&amp; test "$before_hashes" = "$after_hashes" &amp;&amp; go test ./internal/receipt -run 'Test(ReferenceEncoderAgreement|GoldenFixtures)' -count=1 &amp;&amp; go test -race ./internal/receipt -count=1 &amp;&amp; test -z "$(find internal/receipt -name '*_test.go' -type f -exec grep -lE 'os\.(WriteFile|Create|CreateTemp|OpenFile|Mkdir|MkdirAll|Rename|Remove|RemoveAll)|O_(CREATE|TRUNC|WRONLY|RDWR)' {} +)" &amp;&amp; test -z "$(find internal/receipt -name '*_test.go' -type f -exec grep -lE 'go:generate|UPDATE_GOLDEN|testdata/v1/gen|gen/main\.go|os/exec|exec\.Command' {} +)" ) &amp;&amp; ! grep -E 'receipt\.(Validate|CanonicalHashInput|ComputeEntryHash)' internal/receipt/testdata/v1/gen/main.go &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - D-23: Manifest and binary cover genesis linkage, Unicode, empty delegation, maximum integers, and validation boundaries.
    - D-23: Manifest length and documented SHA-256 match the immutable binary bytes.
    - D-24: Tests never write files. The generator is explicit, independent, and refuses existing destinations.
    - D-24: No update flag, environment rewrite path, `go:generate`, or test-time generator call exists.
    - The generator's second run fails before replacing either reviewed fixture.
    - Fixture data contains none of the prohibited privacy sentinels.
    - The task commit uses `git commit -s` and normal hooks. It never uses `--no-verify`.
  </acceptance_criteria>
  <done>Every listed acceptance criterion passes, and the automated verification exits zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validated Receipt to canonical bytes | Structured fields cross into a security-sensitive binary grammar. |
| Production encoder to black-box encoder | Two implementations must agree without shared encoding logic. |
| Generator to reviewed fixtures | An explicit tool writes protocol evidence that tests may only read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02 | Tampering | `CanonicalHashInput` | mitigate | Fix domain, widths, lengths, counts, order, and exclusions. Compare with an independent encoder. |
| T-04 | Tampering | Golden manifest and binary | mitigate | Keep tests read-only. Use fixed hashes and an exclusive no-overwrite generator. |
</threat_model>

<source_coverage>
| Source | IDs | Plan coverage |
|--------|-----|---------------|
| GOAL | one deterministic auditor contract | Production, independent, and golden bytes agree. |
| REQ | RCPT-01 | Exact domain, field order, widths, lengths, bounds, and exclusions. |
| REQ | RCPT-04 | External-package reference encoder agrees byte-for-byte. |
| REQ | RCPT-05 | Manifest and binary cover every required boundary. |
| RESEARCH | append-only grammar, fuzzing, immutable fixture architecture | Tasks 1 through 3 implement each constraint. |
| CONTEXT | D-01 through D-11 and D-20 through D-24 | Must-haves and task acceptance criteria name every decision. |
</source_coverage>

<verification>
Run the complete Phase 2 gate:

`go test ./...`

`go vet ./...`

`go test -race ./internal/receipt -count=1`

`go test ./internal/receipt -run '^$' -fuzz '^FuzzDigestParams$' -fuzztime=10s`

`go test ./internal/receipt -run '^$' -fuzz '^FuzzCanonicalHashInput$' -fuzztime=10s`

`git diff --check`

Confirm only the 14 planned Phase 2 implementation paths changed. Confirm no gateway or audit logger edits. Confirm every task commit uses `git commit -s` with normal hooks.
</verification>

<success_criteria>
- The production preimage exactly implements the domain and grammar copied from CONTEXT.md.
- Independent bytes equal production bytes before hash comparison.
- ComputeEntryHash equals SHA-256 over the complete canonical preimage.
- Both fuzz targets pass their 10-second phase checks.
- Manifest, binary length, and documented SHA-256 agree.
- Tests cannot rewrite fixtures. The explicit generator refuses overwrite.
- No signing, storage, gateway, audit logger, export, verifier, or CLI work enters Phase 2.
- RCPT-01 through RCPT-05 have automated evidence across both plans.
</success_criteria>

<output>
Create `.planning/phases/02-receipt-protocol/02-02-SUMMARY.md` when done.
</output>
