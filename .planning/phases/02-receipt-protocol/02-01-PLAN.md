---
phase: 02-receipt-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/receipt/receipt.go
  - internal/receipt/receipt_test.go
  - internal/receipt/params.go
  - internal/receipt/params_test.go
autonomous: false
requirements:
  - RCPT-01
  - RCPT-02
  - RCPT-03
must_haves:
  truths:
    - "D-06: D-07: D-08: D-09: D-10: D-11: Receipt fields retain fixed Go types, byte limits, ranges, UTF-8 rules, ASCII rules, and snapshot semantics."
    - "D-12: D-13: D-14: D-15: D-16: DigestParams strictly canonicalizes raw object JSON with RFC 8785 and returns only a SHA-256 digest."
    - "D-17: D-18: D-19: Policy and error vocabularies prevent request data, tokens, bodies, and provider text from entering receipts."
    - "D-20 and D-21: The package exposes pure protocol functions and excludes signing, storage, middleware, exports, and gateway wiring."
    - "The pinned JSON dependency is installed only after the required static scan reaches an allowed disposition."
  artifacts:
    - path: "internal/receipt/receipt.go"
      provides: "Receipt field contract, validation, stable errors, and private snapshots"
      exports: ["Receipt", "Validate"]
    - path: "internal/receipt/params.go"
      provides: "Strict raw JSON validation and RFC 8785 parameter digest"
      exports: ["DigestParams"]
    - path: "internal/receipt/receipt_test.go"
      provides: "Reflection, boundary, vocabulary, and snapshot contract tests"
    - path: "internal/receipt/params_test.go"
      provides: "Canonicalization, parser rejection, resource bound, and privacy tests"
    - path: "go.mod"
      provides: "Exact direct dependency pin for github.com/go-json-experiment/json"
      contains: "v0.0.0-20251027170946-4849db3c2f7e"
  key_links:
    - from: "internal/receipt/params.go"
      to: "github.com/go-json-experiment/json/jsontext"
      via: "one strict token and canonicalization path"
      pattern: "jsontext"
    - from: "internal/receipt/params.go"
      to: "crypto/sha256"
      via: "digest of bounded RFC 8785 bytes"
      pattern: "sha256.Sum256"
    - from: "internal/receipt/receipt_test.go"
      to: "internal/receipt/receipt.go"
      via: "exact reflected field contract and table-driven validation"
      pattern: "Test(ReceiptFieldContract|Validate|Snapshot)"
---

<objective>
Establish the strict semantic receipt contract and privacy-limited parameter digest.

Purpose: Make every later byte encoder operate on one validated, immutable snapshot.
Output: A pinned parser dependency, Receipt model, validation, DigestParams, and focused tests.

This plan and 02-02 remain execution slices inside one future R2 pull request.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-apache-2-0-release-basis/01-01-SUMMARY.md
@.planning/phases/02-receipt-protocol/02-CONTEXT.md
@.planning/phases/02-receipt-protocol/02-RESEARCH.md
@.planning/phases/02-receipt-protocol/02-VALIDATION.md
@.planning/phases/02-receipt-protocol/02-PATTERNS.md
@PRD-receipts-oss.md
@CLAUDE.md
@.github/copilot-instructions.md

<interfaces>
Create this exact public semantic type in protocol order:

type Receipt struct {
    Seq               uint64
    TimestampUnixNS   uint64
    HumanPrincipal    string
    AgentKeyID        string
    DelegationChain   []string
    Service           string
    Action            string
    ParamsSHA256      [32]byte
    PolicyDecision    string
    StatusCode        int
    LatencyMS         int64
    Error             string
    PrevHash          [32]byte
    EntryHash         [32]byte
    SignerKID         string
    Signature         [64]byte
}

The plan must expose these pure functions:

func Validate(receipt Receipt) error
func DigestParams(raw []byte) ([32]byte, error)
</interfaces>
</context>

<tasks>

<task type="auto" may_checkpoint="true">
  <name>Task 1: Gate and pin the RFC 8785 dependency</name>
  <read_first>
    .planning/phases/02-receipt-protocol/02-RESEARCH.md
    .planning/phases/02-receipt-protocol/02-VALIDATION.md
    .planning/phases/02-receipt-protocol/02-PATTERNS.md
    go.mod
    go.sum
  </read_first>
  <files>go.mod, go.sum</files>
  <action>
Inspect commit `4849db3c2f7e2cc8a9816ebf68aafb0a046dec5b` without executing package installer code. Fetch the source into a temporary directory. Verify the checked-out HEAD equals that commit. Run `skillspector scan <source> --no-llm --format json`. Do not enable LLM analysis. Inspect the full JSON result and all inspected-file counts.

On `SAFE`, continue automatically. On `CAUTION`, stop at a blocking human decision checkpoint. Present every finding, the immutable commit, BSD-3-Clause license, and expected checksum. Continue only after explicit approval. On `DO_NOT_INSTALL`, scan error, incomplete analysis, or any uninspected file, stop the plan as blocked. Never run `go get` before this disposition gate passes.

After an allowed disposition, verify the pinned source license is BSD-3-Clause. Run `go get github.com/go-json-experiment/json@v0.0.0-20251027170946-4849db3c2f7e`. Keep `go 1.25.0` unchanged. Keep the existing 2-block require layout. Verify the module checksum equals `h1:Lf/gRkoycfOBPa42vU2bbgPurFong6zXeFtPoxholzU=`. Do not alter unrelated dependency lines.
  </action>
  <verify>
    <automated>test "$(go list -m -f '{{.Version}}' github.com/go-json-experiment/json)" = "v0.0.0-20251027170946-4849db3c2f7e" &amp;&amp; go mod download -json github.com/go-json-experiment/json@v0.0.0-20251027170946-4849db3c2f7e | grep -F 'h1:Lf/gRkoycfOBPa42vU2bbgPurFong6zXeFtPoxholzU=' &amp;&amp; grep -F 'go 1.25.0' go.mod &amp;&amp; git diff --check -- go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - The scan targets exactly commit `4849db3c2f7e2cc8a9816ebf68aafb0a046dec5b`.
    - SAFE proceeds automatically. CAUTION has explicit human approval before installation.
    - DO_NOT_INSTALL, scan failure, incomplete analysis, or uninspected files block installation.
    - The pinned source has a verified BSD-3-Clause license.
    - go.mod uses pseudo-version `v0.0.0-20251027170946-4849db3c2f7e`.
    - go.sum contains checksum `h1:Lf/gRkoycfOBPa42vU2bbgPurFong6zXeFtPoxholzU=`.
    - No package command runs before the static scan gate passes.
  </acceptance_criteria>
  <done>Every listed acceptance criterion passes, and the automated verification exits zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Receipt validation and snapshot semantics</name>
  <read_first>
    .planning/phases/02-receipt-protocol/02-CONTEXT.md
    .planning/phases/02-receipt-protocol/02-PATTERNS.md
    PRD-receipts-oss.md
    internal/auth/keys.go
    internal/vault/vault.go
  </read_first>
  <files>internal/receipt/receipt.go, internal/receipt/receipt_test.go</files>
  <behavior>
    - Reflection reports the exact 17 fields, order, and Go types shown in the interface contract.
    - Every lower and upper boundary follows D-09 through D-11 without coercion.
    - Exact UTF-8 bytes remain unchanged. Invalid UTF-8 and NUL fail.
    - PolicyDecision accepts only allow, deny, and rate_limited.
    - Error accepts empty or `^[a-z0-9_]{1,64}$`.
    - Public operations isolate DelegationChain through a private snapshot.
  </behavior>
  <action>
Write `receipt_test.go` first. Confirm focused tests fail because the package contract is absent. Add the Apache header. Define `Receipt` exactly as shown above. Keep field order unchanged. Add stable `receipt:` sentinel errors. Public errors must not include rejected values.

Implement `Validate(Receipt) error`. Snapshot the struct and clone `DelegationChain` before checks. Require `seq >= 1`, `timestamp_unix_ns >= 1`, `100 <= status_code <= 599`, and `latency_ms >= 0`. Never clamp or coerce.

Require nonempty `human_principal`, `agent_key_id`, `service`, `action`, `policy_decision`, and `signer_kid`. Their byte limits are exactly 256, 128, 64, 128, 16, and 128. Require valid UTF-8, reject NUL, and perform no Unicode normalization.

Allow 0 through 32 ordered delegation elements. Each element may be empty but cannot exceed 64 ASCII bytes. Error is empty or 1 through 64 lowercase ASCII machine-code bytes matching D-18. Keep signing, key lifecycle, persistence, middleware, exports, clocks, and gateway code outside this package.
  </action>
  <verify>
    <automated>go test ./internal/receipt -run 'Test(ReceiptFieldContract|Validate|Snapshot)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - D-06: Receipt retains the PRD types. Numeric validation precedes later encoding conversions.
    - D-07: String limits use UTF-8 bytes. NUL and invalid UTF-8 fail. No normalization occurs.
    - D-08: DelegationChain is ordered, allows empty elements, and supports 0 through 32 elements.
    - D-09: Sequence, timestamp, status, and latency ranges are exact. Invalid values are not coerced.
    - D-10: Required strings and their 256, 128, 64, 128, 16, and 128 byte limits are exact.
    - D-11: Delegation elements and Error enforce exact ASCII and 64-byte limits.
    - D-17: PolicyDecision accepts exactly allow, deny, or rate_limited.
    - D-18: Error contains only an empty value or a stable lowercase machine code.
    - D-19: The type contains no raw parameters, OAuth tokens, upstream bodies, or unrestricted provider errors.
    - D-20: Validation is pure, returns errors, and does not mutate caller slices.
    - D-21: No signing, key, database, middleware, export, gateway, audit logger, or clock code is added.
  </acceptance_criteria>
  <done>Every listed acceptance criterion passes, and the automated verification exits zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement strict parameter digests and privacy tests</name>
  <read_first>
    .planning/phases/02-receipt-protocol/02-CONTEXT.md
    .planning/phases/02-receipt-protocol/02-RESEARCH.md
    .planning/phases/02-receipt-protocol/02-PATTERNS.md
    internal/receipt/receipt.go
    go.mod
  </read_first>
  <files>internal/receipt/params.go, internal/receipt/params_test.go</files>
  <behavior>
    - Nil, empty, JSON whitespace, and exact null digest as the empty object.
    - Equivalent object key order and whitespace produce the same digest.
    - Duplicate decoded names, invalid Unicode, noncharacters, overflow, and trailing JSON fail.
    - Root depth 32 passes. Depth 33 fails.
    - Canonical output of exactly 1 MiB passes. One byte more fails.
    - Returned errors and receipt artifacts never expose sensitive sentinels.
  </behavior>
  <action>
Write strict parser, RFC 8785, boundary, and privacy tests before implementation. Confirm focused tests fail. Implement `DigestParams(raw []byte) ([32]byte, error)` with one pinned `jsontext` path.

Normalize only nil, empty, JSON whitespace, and exact JSON `null` to `{}`. Require one top-level object and EOF after it. Walk strict tokens before canonicalization. Reject decoded duplicate names, invalid UTF-8, malformed surrogates, Unicode noncharacters, nesting beyond 32, and trailing values. Parse each number with `strconv.ParseFloat(..., 64)`. Reject range errors, including `1e309`. Do not reject representable `9007199254740992` solely for exceeding the safe-integer range.

Clone normalized bytes before `jsontext.Value.Canonicalize`, because canonicalization mutates its receiver. Reject canonical output above `1 << 20` bytes. Return only `sha256.Sum256(canonical)`. Never retain or expose raw or canonical JSON. Convert dependency failures into stable local sentinel categories. Do not wrap parser text that may contain names or snippets.

Add privacy sentinels for raw parameters, OAuth tokens, upstream bodies, and provider text. Search returned errors, Receipt state, and any available protocol bytes. Do not edit `internal/gateway` or `internal/audit`.
  </action>
  <verify>
    <automated>go test ./internal/receipt -run 'Test(DigestParams|Privacy)' -count=1 &amp;&amp; go test -race ./internal/receipt -count=1 &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - D-12: RFC 8785 canonicalization uses the pinned maintained package. Number and string rules are not hand-rolled.
    - D-13: DigestParams accepts raw JSON bytes and rejects duplicates before map collapse.
    - D-14: Omitted, whitespace-only, and null inputs normalize to `{}`. Other top-level values fail.
    - D-15: Duplicate names, invalid Unicode, non-I-JSON numbers, depth above 32, and output above 1 MiB fail.
    - D-16: DigestParams returns only `[32]byte`. Receipt stores neither raw nor canonical parameter JSON.
    - D-18: Parser and validation errors expose no provider messages, response bodies, tokens, or request content.
    - D-19: Privacy sentinel tests cover every prohibited data class.
    - D-20: DigestParams does not mutate caller bytes and returns stable local errors.
    - Focused tests and the receipt race suite pass.
    - The task commit uses `git commit -s` and normal hooks. It never uses `--no-verify`.
  </acceptance_criteria>
  <done>Every listed acceptance criterion passes, and the automated verification exits zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw parameter bytes to strict JSON tokens | Untrusted caller bytes enter protocol parsing. |
| Parsed JSON to RFC 8785 bytes | One interpretation must survive validation and canonicalization. |
| Receipt values to semantic validation | Caller-owned strings and slices cross into protocol state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Tampering | `DigestParams` | mitigate | Use one pinned strict parser. Reject duplicates, invalid Unicode, trailing values, and semantic ambiguity. |
| T-03 | Information Disclosure | Receipt and parser errors | mitigate | Store digests only. Use closed errors and scan sentinels across state, bytes, and errors. |
| T-05 | Denial of Service | JSON token and canonicalization path | mitigate | Reject depth above 32 and canonical output above 1 MiB. Fuzz parser boundaries in 02-02. |
</threat_model>

<source_coverage>
| Source | IDs | Plan coverage |
|--------|-----|---------------|
| GOAL | deterministic contract and sensitive-data omission | Semantic type, strict digest, validation, and privacy tests. |
| REQ | RCPT-01 | Fixed fields, types, ranges, limits, and vocabularies. |
| REQ | RCPT-02 | Strict raw JSON and RFC 8785 SHA-256 digest. |
| REQ | RCPT-03 | Digest-only storage and sanitized error behavior. |
| RESEARCH | strict pre-pass, snapshot semantics, stable errors | Tasks 2 and 3 implement each constraint. |
| CONTEXT | D-06 through D-21 | Must-haves and task acceptance criteria name every decision. |
</source_coverage>

<verification>
Run after this wave:

`go test -race ./internal/receipt -count=1`

Confirm no runtime gateway or audit logger file changed. Confirm every task commit has a sign-off trailer. Use normal hooks.
</verification>

<success_criteria>
- The dependency gate is satisfied or the plan stops with a documented blocker.
- Receipt fields and validation match D-06 through D-11 and D-17 through D-20.
- Equivalent decoded objects produce the same SHA-256 digest.
- Ambiguous or excessive JSON fails with sanitized local errors.
- Receipt state and errors contain no prohibited sensitive data.
- No gateway, audit logger, signing, persistence, export, or CLI code changes.
</success_criteria>

<output>
Create `.planning/phases/02-receipt-protocol/02-01-SUMMARY.md` when done.
</output>
