services:
  calendly:
    base_url: https://api.calendly.com
    auth:
      type: bearer
    actions:
      list_scheduled_events:
        method: GET
        path: /scheduled_events
        params:
          user: string
          count: "int?"